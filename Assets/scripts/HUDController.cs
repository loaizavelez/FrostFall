using UnityEngine;
using TMPro;  

public class HUDController : MonoBehaviour
{

    /*[Header("Información del jugador")]
    public TextMeshProUGUI infoText;   // arrastra aquí tu HUDText desde el inspector
    public PlayerMovement player;    // referencia al jugador

    void Update()
    {
        if (player != null && infoText != null)
        {
            float velocidad = player.GetVelocidad();   // método en PlayerMovement
            float distancia = player.GetDistancia();   // método en PlayerMovement

            infoText.text = "Velocidad: " + velocidad.ToString("F2") +
                            " | Distancia: " + distancia.ToString("F2");
        }
    }*/
}
